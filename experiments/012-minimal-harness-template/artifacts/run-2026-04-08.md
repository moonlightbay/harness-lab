# Experiment 012 Run Record

Date: 2026-04-08

Context:

- The template was refined after two days of use inside another project.
- The main additions were state compression, next-action control, wave execution, fixed closeout gates, cadence review, upstream sync handling, legacy code classification, and harness self-improvement via rules, checks, and skills.

Commands used:

1. `python checks/check-top-level-guidance.py .`
2. `python checks/check-architecture.py .`
3. `python validate-template.py`

Artifacts generated:

- `artifacts/template-guidance-check.json`
- `artifacts/template-architecture-check.json`
- `artifacts/template-validation.json`

Validation outcome:

- verdict: `pass`
- total checks passed: `16 / 16`
- top-level pack words: `287`
- required top-level paths covered: `6 / 6`
- stale references: `0`
- authority conflicts: `0`

Template highlights:

- short top-level map
- `agent-state` and `next-action` as first-class docs
- explicit wave planning with allowed files and stop conditions
- fixed quality gate for diff review, checks, verification, log update, and local commit
- five-commit branch review cadence
- upstream sync handled as a translation task
- harness improvement loop that promotes recurring friction into rules, checks, or skills
