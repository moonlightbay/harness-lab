# Experiment 012 Run Record

Date: 2026-04-10

Context:

- The template was simplified to better complement Codex's built-in harness.
- Explicit checked-in state, next-action, plan, workflow, and skill layers were removed.
- Durable context was compressed into `docs/project.md`, `docs/architecture.md`, `docs/task.md`, and `docs/log.md`.

Commands used:

1. `python checks/check-top-level-guidance.py .`
2. `python checks/check-architecture.py .`
3. `python validate-template.py`

Artifacts generated:

- `artifacts/template-guidance-check.json`
- `artifacts/template-architecture-check.json`
- `artifacts/template-validation.json`

Validation outcome:

- verdict: `pass`
- total checks passed: `12 / 12`
- top-level pack words: `183`
- required top-level paths covered: `5 / 5`
- stale references: `0`
- authority conflicts: `0`

Template highlights:

- four durable docs: `project`, `architecture`, `task`, and `log`
- short top-level map in `AGENTS.md` and `README.md`
- repo-specific architecture rule hook kept in `checks/`
- lower read cost by removing redundant checked-in process docs
