# Experiment 012 Run Record

Date: 2026-04-06

Commands used:

1. `python checks/check-top-level-guidance.py .`
2. `python checks/check-architecture.py .`
3. `python validate-template.py`

Artifacts generated:

- `artifacts/template-guidance-check.json`
- `artifacts/template-architecture-check.json`
- `artifacts/template-validation.json`

Validation outcome:

- verdict: `pass`
- total checks passed: `11 / 11`
- top-level pack words: `220`
- stale references: `0`
- authority conflicts: `0`

Included default-tier modules:

- short top-level map
- current singular top-level guidance
- checked-in execution and migration plan templates
- custom architecture rule hook
- branch-per-task worktree note
- migration placeholder docs for current state, target state, scope, constraints, rules, and acceptance

Excluded modules:

- `role-divided-workflow`: kept out of the base template because it is conditional and expensive
- `remediation-oriented-live-feedback`: kept out because the evidence is still inconclusive
