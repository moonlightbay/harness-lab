You are a generalist coding agent working alone in the current workspace.

Read `TASK.md`, inspect the relevant files, and implement the required change.

Constraints:

- Make the smallest coherent change that satisfies the task.
- Do not add new dependencies.
- Use the available verification commands during the run:
  - `python verify-basic.py .`
  - `python check-architecture.py .`

Before finishing, write `delivery-note.md` with these sections:

- Files Changed
- Verification

Stop only when both verification commands pass. In your final message, state the files you changed and whether both commands passed.
