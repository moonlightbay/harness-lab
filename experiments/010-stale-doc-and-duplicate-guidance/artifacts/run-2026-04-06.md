# Experiment 010 Run Record

Date: 2026-04-06

Commands used:

1. `score-legibility-pack.ps1` for the baseline pack (`AGENTS.md` + `README.md`)
2. `score-legibility-pack.ps1` for `fixtures/STALE-TOP-LEVEL-MAP.md`
3. `score-legibility-pack.ps1` for `fixtures/DUPLICATE-GUIDANCE-MAP.md`
4. `compare-legibility-packs.ps1` to assemble the final comparison

Results:

- baseline: `12 / 12`
- stale-doc: `6 / 12`
- duplicate-guidance: `8 / 12`
- overall verdict: `coverage-alone-does-not-protect-agent-legibility`

Observed details:

- All three packs preserved `4 / 4` required-path coverage.
- The stale-doc fixture introduced two dead authority paths:
  - `docs/plans/current-roadmap.md`
  - `docs/plans/run-log.md`
- The duplicate-guidance fixture introduced two conflicting authority topics:
  - `active roadmap`
  - `experiment log`
- The stale-doc fixture was shorter than the baseline but still scored much worse, which shows that small top-level context is only good when it stays current and singular.
